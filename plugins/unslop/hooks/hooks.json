{
  "description": "unslop: inject the anti-slop rules at session start (scoped wrapper around the vendored skill).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/inject-unslop.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
