{
  "name": "flow",
  "version": "0.31.0",
  "description": "The prep → issue → land development pipeline: framework skill, commands, workflow, hooks, and the engineering charter.",
  "author": {
    "name": "jakub"
  },
  "mcpServers": {
    "flow_delegate": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/delegation.mjs",
        "mcp",
        "--host",
        "claude"
      ],
      "timeout": 7500000
    }
  }
}
