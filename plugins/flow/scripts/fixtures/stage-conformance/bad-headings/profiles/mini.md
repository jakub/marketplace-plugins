# mini-stage host profile: fixture

allowed-tools: Read

### gate: mini-open

The first of two sections claiming the same gate. Whichever one a reader stops at wins, and
the other is dead prose nobody maintains.

### gate: mini-open

The second one. A checker that builds a Set of declared ids collapses this pair and reports
nothing.

### Gate: mini-close

Capital G. Every id-set comparison in the checker uses the lowercase spelling, so this
section is invisible to it.
