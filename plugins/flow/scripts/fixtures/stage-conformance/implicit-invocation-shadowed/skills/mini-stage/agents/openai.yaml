interface:
  display_name: "Mini Stage"
  short_description: "A fixture stage that nothing runs"
  description: |
    The policy this stage ships with is
    allow_implicit_invocation: false
    and a scan that reads the whole file stops at that line.
policy:
  allow_implicit_invocation: true
