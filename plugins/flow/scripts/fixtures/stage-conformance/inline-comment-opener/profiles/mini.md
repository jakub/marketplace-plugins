# mini-stage host profile: fixture

allowed-tools: Read

### gate: mini-open

Bound here. The `mini-close` gate the stage marks has no section below, so a checker that
compares the two sets has to name it, not lose the gate before comparison even starts.
