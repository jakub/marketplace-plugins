must DENY
  ok: Co-Authored-By → DENY (want DENY)
  ok: Claude-Session → DENY (want DENY)
  ok: git -C … commit + trailer → DENY (want DENY)
  ok: robot trailer → DENY (want DENY)
  ok: Generated-with trailer → DENY (want DENY)
  ok: commit --no-verify → DENY (want DENY)
  ok: push --no-verify → DENY (want DENY)
  ok: compound cmd + trailer → DENY (want DENY)
  ok: bare force-push → DENY (want DENY)
  ok: force-push short flag → DENY (want DENY)
  ok: force flag trailing → DENY (want DENY)
  ok: force-push after a read → DENY (want DENY)
  ok: checkout bare dot → DENY (want DENY)
  ok: restore bare dot → DENY (want DENY)
  ok: clean force → DENY (want DENY)
  ok: clean --force → DENY (want DENY)
  ok: clean force in a flag cluster → DENY (want DENY)
cron mode (FLOW_CRON_JOB) - deny-by-default git
  ok: lint: push → DENY (want DENY)
  ok: lint: push to arbitrary remote → DENY (want DENY)
  ok: lint: remote add → DENY (want DENY)
  ok: lint: commit → DENY (want DENY)
  ok: lint: sanction string ignored → DENY (want DENY)
  ok: lint: branch rename → DENY (want DENY)
  ok: lint: config → DENY (want DENY)
  ok: lint: branch -Df bundled delete → DENY (want DENY)
  ok: lint: branch create (positional) → DENY (want DENY)
  ok: lint: branch create with start-point → DENY (want DENY)
  ok: lint: symbolic-ref repoint → DENY (want DENY)
  ok: lint: symbolic-ref delete → DENY (want DENY)
  ok: lint: fetch writing refspec → DENY (want DENY)
  ok: lint: push glued to && after a read → DENY (want DENY)
  ok: lint: branch delete glued to && → DENY (want DENY)
  ok: lint: push behind an unspaced pipe → DENY (want DENY)
  ok: lint: push in a command substitution → DENY (want DENY)
  ok: lint: push quoted inside bash -c → DENY (want DENY)
  ok: lint: push quoted inside sh -c → DENY (want DENY)
  ok: lint: the real branch-audit command → allow (want allow)
  ok: lint: quoted format string still reads → allow (want allow)
  ok: lint: branch --merged <ref> read → allow (want allow)
  ok: lint: branch --contains read → allow (want allow)
  ok: lint: branch -a -v read → allow (want allow)
  ok: lint: bare branch list → allow (want allow)
  ok: lint: symbolic-ref read → allow (want allow)
  ok: lint: fetch prune read → allow (want allow)
  ok: lint: branch -D goes through executor → DENY (want DENY)
  ok: lint: worktree remove goes through executor → DENY (want DENY)
  ok: lint: worktree prune goes through executor → DENY (want DENY)
  ok: lint: log read → allow (want allow)
  ok: lint: rev-list read → allow (want allow)
  ok: lint: remote get-url → allow (want allow)
  ok: lint: fetch → allow (want allow)
  ok: lint: branch list → allow (want allow)
  ok: sweep: branch -D denied → DENY (want DENY)
  ok: sweep: worktree remove denied → DENY (want DENY)
  ok: sweep: worktree list → allow (want allow)
  ok: sweep: log read → allow (want allow)
must ALLOW
  ok: clean commit → allow (want allow)
  ok: trailer named mid-subject → allow (want allow)
  ok: auditing history → allow (want allow)
  ok: git log --grep=commit → allow (want allow)
  ok: grepping repo → allow (want allow)
  ok: --no-verify named in a PR comment → allow (want allow)
  ok: --no-verify inside single quotes → allow (want allow)
  ok: --no-verify named in a commit subject → allow (want allow)
  ok: --no-verify in a heredoc body → allow (want allow)
  ok: sanctioned amend → allow (want allow)
  ok: force-with-lease → allow (want allow)
  ok: force-with-lease, no refspec → allow (want allow)
  ok: checkout a path starting with ./ → allow (want allow)
  ok: checkout -- path → allow (want allow)
  ok: restore a named path → allow (want allow)
  ok: clean dry run → allow (want allow)
  ok: clean dry run with -d → allow (want allow)
  ok: clean --dry-run → allow (want allow)
  ok: force-push named in prose → allow (want allow)
  ok: non-git → allow (want allow)
  ok: gh → allow (want allow)

git-guard: ALL PASS
